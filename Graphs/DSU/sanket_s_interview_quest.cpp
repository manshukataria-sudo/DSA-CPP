#include <iostream>
#include <vector>

using namespace std;
string canReach(vector<string>& map){
    int i , j = 0;
    
    while(visited.count(map[i][j])){}
}
int main() {
    vector<string> map = {"rrdr", "dldl", "udld", "urrr"};
    cout << canReach(map);
    cout << '\n';
    return 0;
}