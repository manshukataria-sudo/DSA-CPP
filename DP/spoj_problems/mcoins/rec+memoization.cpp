#include <iostream>
#include <vector>
using namespace std;
int main() {
    int K, L, m;                // can choose form 1, K, L coins
    cin >> K >> L >> m;
    vector<int> arr(m);
    for (int i = 0; i < m; i++) {
        cin >> arr[m];
    }

}